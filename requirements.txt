# Servidor central (API + painel). Python 3.10 ou mais novo.
# Dependências do dispositivo de monitoramento ficam em requirements-device.txt.
fastapi==0.141.1
uvicorn==0.52.4
websockets==17.1
sqlalchemy==2.0.52
pydantic==2.13.5
pytz==2026.3.post1
# Driver do Postgres: só é usado quando DATABASE_URL aponta para um Postgres.
psycopg[binary]==3.3.5
