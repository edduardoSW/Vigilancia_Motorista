import { HomeScreen } from "@/components/home/home-screen";

// Início (spec 014): Primeiros passos, caixa conectada, o que falta verificar, avisos e o teste neste computador.
export default function HomePage() {
  return <HomeScreen />;
}
